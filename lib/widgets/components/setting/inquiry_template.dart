import 'package:flutter/material.dart';
import 'package:reading_memo/widgets/components/common/basic_app_bar.dart';

class InquiryTemplate extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: BasicAppBar(
        title: "お問い合わせ",
      ),
      body: Container(),
    );
  }
}
